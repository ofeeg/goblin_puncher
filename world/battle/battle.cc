#include "battle.hh"


void Battle::InitPassive(Combatant& a){
  if(!(a.GetPassiveAbilities.compare("")){
    for(Ability*  i : a.Abilities.first){
      if(i->ScaleType = TYPES::PAS){
        std::cout << "Doing something unique \n";
       }
       else{
	  std::cout << i->ScaleDamage;
	}
     }
   }
}
